app:
  rabbitmq:
    queue:
      status-falha: pedidos.status.falha.emerson
      status-sucesso: pedidos.status.sucesso.emerson
      entrada-dlq: pedidos.entrada.emerson.dlq
      entrada: pedidos.entrada.emerson
    exchange:
      pedidos: pedidos.exchange
# Configurações do RabbitMQ
spring:
  rabbitmq:
    password: guest
    port: '5672'
    host: localhost
    username: guest
  application:
    name: pedidos
server:
  port: '8080'
